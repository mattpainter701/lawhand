# LawHand future core UX and features: refinement epic

Status: draft for ongoing collaboration. Research and an initial walkthrough are complete; product implementation has not started. Keep this planning PR in draft until the user requests otherwise.
Owner: “Plan legal platform UX refinement” — future state of core UX and features, assigned by the user on 22 September 2026.
Date: 22 September 2026.

## Outcome

Help staff open the right matter, see what needs attention, record work, and return to their queue without rebuilding their context. Preserve LawHand’s current visual identity and familiar destinations. Give users a useful starting arrangement plus a few easy ways to change it.

Working audience: paralegals and legal assistants with limited comfort learning software; attorneys and intake staff are secondary validation groups. This is an assumption pending the user's preference, not a finding from user interviews. The user's report that staff feel overwhelmed is the problem statement. Competitor reviews and an expert walkthrough suggest causes; usability sessions must test those hypotheses.

The first release should make daily work clearer before asking anyone to configure anything. Personalization must stay optional. Success means fewer decisions and less searching to finish the same work, not simply fewer visible controls.

## Core fundamentals for the larger epic

**Product promise:** staff can find the right record, understand what needs attention, take the next action, and return to their work with confidence. The application should explain where they are, what a control will do, who owns the next step, and whether the action succeeded.

This is the proposed product outline for collaboration after the parity and market-evidence commits 5f0e0c07, b05e40a6 and 9bdd7e7c. The fundamentals describe the desired experience; the existing UX story IDs remain the implementation backlog. They do not authorize a visual rebrand or simultaneous implementation of every competitor feature.

### Eight fundamentals

| Fundamental | What staff should experience | Concrete work and evidence | Completion signal |
| --- | --- | --- | --- |
| **F1. A familiar, adaptable workspace** | The main places and words make sense immediately; staff can adjust the arrangement without becoming designers. | Make My Matters, Tasks, Calendar and Clients easy to reach; clarify Clients/Contacts/Intake; offer an optional everyday preset; finite section order/collapse and column choices; consistent Customize view and reset. Preserve existing choices and state whether each preference follows the account or device. UX-01/02/07; G-01/18/26/27. | A novice reaches the requested destination unaided. Defaults work without configuration; a user can adjust and restore a view without changing business data or permissions. |
| **F2. A matter that explains the case** | Opening a matter answers: who is the client, who is responsible, what is next, when is it due, and what is waiting? | Compact identity, next work and key dates ahead of empty/setup forms; concise recent activity, communication and document-status summaries; preserve context into every action. UX-01/02; G-01/02. Document owner supplies preparation state and actions. | Pass the “Find a matter and its next deadline” scenario in [Usability validation](#usability-validation); required alerts remain visible when sections collapse. |
| **F3. A clear daily work queue** | Staff and supervisors can distinguish overdue, due today, upcoming, waiting and unassigned work, then act on it. | Build on My/Firm Tasks and matter-linked work: actionable counters, explicit ownership, due dates/times, waiting reasons and review state; searchable queues that retain position. Every displayed count opens the records it counts. Avoid a second competing task store or another dashboard to learn. UX-03/04/08; G-09/10/11. | Pass “Decide what to do today” in [Usability validation](#usability-validation). Completing, assigning or waiting an item updates the same queue; record totals stay consistent. |
| **F4. A calendar staff can trust** | People understand what kind of date they see, whose calendar it belongs to, and what changing it affects. | Accurate range labels; useful agenda; matter/type/source filters; distinguish deadlines, appointments and work blocks; explain local save versus external sync; keyboard date editing. Keep current deduplication and explicit rescheduling intent. UX-05; G-14–17. | Blocking work time never moves the legal deadline. A failed provider sync cannot look successful; participants can locate and correct the relevant item without dragging. |
| **F5. One continuous client journey** | A call or inquiry becomes a follow-up and, when appropriate, a matter without re-entering known facts or losing its history. | Clear Clients/Contacts/Intake relationships, existing-record suggestions, contact and next follow-up first, accessible conflict-check handoff, review before conversion, retry-safe creation, preserved filters and staged activity loading. UX-06/08; G-18–23. Document questionnaires and persisted preparation drafts stay with the document owner. | Pass “Handle a new inquiry for an existing contact” in [Usability validation](#usability-validation); no wrong-matter conversion or accidental engagement. |
| **F6. Find work and resume it** | Cross-module search covers permitted matters, clients and contacts; opening one and returning keeps the user's place. Document-content search has a separate owner. | Complete matter/client/contact lists; bounded task queries; documented search fields; URL-backed scope/filter/page, return position and normal links. Add cross-module record search as a later coherent slice, reusing existing endpoints where appropriate. UX-04/08/10; G-03/04/10/21/23/24. | A matching record beyond the first page is findable. Back restores the prior queue. Restricted records never appear in results or counts. |
| **F7. Learn an action once** | Adding or editing work follows the same pattern wherever it starts. Routine forms are short, context is visible, and help appears where it is needed. | Shared task composer contract; visible/checkable prefill; consistent required fields, date/time, assignee, validation and button language; optional details disclosed deliberately. Retain input on failed save, prevent duplicate submit and distinguish cancellation from success. Named steps are reserved for longer flows. UX-03/06/07/09; G-09/25/26. | Capturing a callback from either a matter or Tasks produces the same intended record. Users understand the next action without coaching, and a failed request preserves their work. |
| **F8. Clear responsibility and honest state** | A busy office can see who owns work, what is selected for focus, what needs review, and what really completed. | Separate assignment, lifecycle, personal focus, review responsibility and permissions. Resolve UX-01A before focus-list wording ships. Distinguish saving/saved/sync failed, preparation complete/approved/sent/signed, and empty/filtered/failed/incomplete data. Use existing queues/reports for actionable oversight before commissioning new analytics. UX-01A/04/05/09/11; G-11/16 and the working-flag audit. | Staff correctly explain My focus without treating it as presence, a timer or an access switch. An authorized supervisor can trace an exception/count to records. Bulk results identify successes and failures individually. |

### Rules shared by every surface

- **Context travels with the action.** Matter, client, assignee and return target are visible and preserved where appropriate; changing the client/matter invalidates incompatible prefill. A task's responsible person and a document's reviewer/preparer remain distinct.
- **Responsiveness and accessibility are part of completion.** Core identity and actions load without waiting for optional activity. Slow, failed, empty and filtered states are distinguishable. Primary work must function on ordinary laptops, keyboard-only, at 200% zoom and on a narrow viewport; drag is optional.
- **Progressive disclosure must preserve important information.** Move infrequent setup out of the immediate workflow; retain deadlines, unassigned work, required alerts, failed delivery and review gates. Hidden fields retain their values.
- **Actions explain their consequences.** Use specific verbs and plain labels; ask for confirmation when the consequence warrants it, such as a bulk status change or legal-deadline move. Do not add a confirmation dialog to routine navigation. Offer undo only where it can actually restore state.
- **Prefer existing capabilities before new ones.** The parity register distinguishes discoverability, correctness, scale and genuinely missing functionality. Each implementation story names its category and the user outcome it improves.

### How the research informs these priorities

The strongest working hypothesis is that predictable daily work, low setup effort, continuity and visible reliability will help this audience. It is supported by the user's reported overwhelm, LawHand's walkthrough and the recurring complaint themes in the [market evidence](./research/core-ux-market-and-review-evidence-2026-09-22.md). It still requires staff validation.

Apply evidence limits when making scope decisions: ABA's cloud-tool usage mentions do not establish incumbent retention, switching costs or weak entrenchment. Microsoft 365 and practice-management products can be used together. The incompatible ABA/Clio survey figures are not estimates to average, and neither provides a statistical bound for the other. Cross-platform review scores, different archived dates and overlapping review corpora cannot establish an objective product ranking. Funding and customer counts do not prove better UX. These qualifications govern planning where the research narrative goes further.

Reporting complaints justify checking whether supervisors can answer practical questions such as “what is overdue, unassigned or waiting?” using existing queues and reports. They do not yet justify a new report builder, universal analytics system or reporting-parity promise. Custom reports, recurring/dependent tasks, named saved views, extra notification rules and richer practice-area configuration require a concrete unmet workflow and a separate estimate before entering the committed scope.

## Ownership and collaboration contract

The user assigned the future state of core UX and features to **Plan legal platform UX refinement**, and document automations to **Validate PDF engine release**. The document-automation owner confirmed this split on 22 September 2026. Ownership is agreed; the proposed UX behavior, priorities and estimates below remain open for collaboration.

| Responsibility | Owner and scope |
| --- | --- |
| Future core UX and features | This epic: My Matters, matter overview hierarchy, work/focus semantics, Tasks, Calendar, CRM directory and lead pipeline, navigation, preferences and cross-module continuity. |
| Document automations | Validate PDF engine release: document questionnaires, template selection/mapping, filling, generation, preview/review, signing/filing, persisted preparation sessions and document-intake draft recovery. |
| Shared product boundary | Core owner defines how staff discover and enter document work and see its status in the matter workspace. Document owner defines the preparation flow, its state/evidence contract and what actions are actually available. Agree the interface before editing either side. |
| Shared implementation files | One editor at a time for MatterDetailPage, shared API exports, navigation and shared backend routers. Coordinate by file and function; directory ownership is not permission to overwrite another active branch. |

The document owner's corrective PRs [#580](https://github.com/mattpainter701/lawhand/pull/580) and [#582](https://github.com/mattpainter701/lawhand/pull/582) have merged; refreshed main is bcecb8cb0df6b9d60167be1bf9e05e15c22f5450. The owner reports both deployed and #582 production-accepted at bcecb8cb as of the updated coordination snapshot on 22 September 2026; this core task has not independently repeated that acceptance. The document owner retains deployment and production acceptance responsibility. The owner reports reconnect/in-app PDF preview corrective work pending in PR #583; Google live storage and packet-save acceptance remain pending its test-account/manual-review requirements. Those are owner-reported dependencies to recheck, not core implementation tasks or new completion claims. Core matter-page, Tasks, Calendar, CRM-directory and navigation ownership stays with this task. Recheck release status at implementation kickoff.

Agreed interface constraints from the document owner:

- Entry links retain the existing /templates/prepare route with template/set, matter, folder and return target. Persisted drafts carry a session in the URL; Resume continues through Documents in progress.
- Changing the source matter clears old-client answers, verification and previews. Manual values must not silently carry to a different client.
- Preserve tenant/matter access checks, verified-field attribution and fill-session preparer ownership. Keep the fill-session owner/tenant/matter/template correlation and serialized autosave/completion contract. Background saving acts as the preparer and uses their preview evidence. Preview generation is not durable save or preparation completion; review must remain accessible without losing interview state.
- Preparation success drains the encrypted draft, records the resulting matter document against the fill session, and removes it from In progress before the existing return/send path. If saving succeeds but closing the session fails, retain the saved file and offer Retry closing this draft without rendering another copy. Preparation completion is distinct from approval, client delivery and signature authority. Consume the canonical [artifact review/delivery contract](./mcp/artifact-review-and-delivery.md) and [document user guide](../frontend/platform_docs/user-guide/09-document-automation-and-esignature.md); implementation must use their current-main versions.
- PR #580 does not change focus semantics, default reviewers or signature permissions. UX-01A must agree a reviewer-responsibility contract with the document owner before changing fallback selection, while retaining reviewer eligibility and review/signature evidence rules.
- UX-02 owns overview shell ordering, collapse state, summaries and entry visibility. It does not replace SignatureRequestsPanel, ClientConversation, their delivery/signing behavior, or document persistence. Required statuses and errors stay discoverable when a panel is collapsed.
- Document-intake durable resume and Gavel-inspired automation/builder parity belong to the document owner. UX-06 owns CRM lead conversion and the handoff; it consumes document state instead of implementing another questionnaire or draft engine.

Coordination is event-driven: share proposed interface changes and conflicting file ownership before implementation; send exact PR/commit links when a dependency lands; jointly validate matter → document work → return/status using synthetic records. Each owner keeps a separate branch/worktree and owns their tests. This draft changes documentation only and does not authorize edits in the document owner's active worktree.

### Reported follow-ups awaiting core UX validation

The document owner reported these observations on 22 September 2026. This task has not independently reproduced them. They inform F1/F6/F8 and remain unscheduled; they do not expand the estimated core implementation scope.

- **Personal cloud recovery:** the current refresh entry is Calendar → Connect Calendar, while personal OAuth callbacks reportedly return users to the admin Cloud area. The document task owns a narrow routing/recovery fix. Core UX should later validate discoverability, personal-versus-firm scope, and returning users to the work that required reconnection. Do not duplicate that routing work or claim it has landed without its PR/acceptance evidence.
- **Cloud Search feedback:** the admin surface reportedly exposes “Live RAG,” raw SEARCH PLAN JSON and uniform numeric scores; broadly matching mail crowded a synthetic filename query. First reproduce with permission-scoped synthetic records and agree the expected results. Future core UX can explain which sources were searched, incomplete coverage, result type and available next actions in ordinary language, with technical diagnostics placed separately. Query interpretation, source weighting and ranking changes require the search owner's agreement and relevance checks; uniform scores alone do not prove the underlying ranking is defective.

These observations are separate from UX-10's proposed matter/client/contact search. They do not authorize broad document-content search, a search-engine rewrite, or changes to the active document release.

## Scope and boundaries

Included: My Matters, matter overview, Tasks, Calendar, Clients & CRM, the existing Intake handoff, and their shared navigation/preferences. Keep colors, typography, component vocabulary, data models, and established routes wherever possible.

Excluded from this epic: a visual rebrand; free-form dashboard/canvas builder; new AI assistant or automation engine; broad global search across documents; billing/trust redesign; replacing the lead/contact model; court-rule deadline calculation; bulk deadline changes; provider migration; PDF filling or signing-engine fixes. Those may be valuable but do not need to be prerequisites for basic workspace clarity.

Target guardrails: changing a view never changes permissions, matter status, assignment, deadlines, consent, or client visibility. The existing working flag violates that separation; UX-01A is an explicit authorization/reviewer-policy change that must establish the new contract before focus is offered as ordinary personalization. Required alerts remain visible even when a related section is collapsed. Clicking a filter never changes business data. Automated work is grouped for readability, never silently completed or hidden from the work queue.

## Evidence baseline

- Production inspected through the authenticated browser at getlawhand.com. Visible build: `329df4e400d6a67e8533d35954f3b90dcb1afe19`, built 2026-09-22 03:30:55 UTC.
- Source reviewed from freshly fetched `origin/main`: `c4d80b46522e1c2b9436fdc8fae343c9d78db8c9`. Production and main are different snapshots; recheck both at implementation kickoff.
- Walkthrough: My Matters list and Columns; a synthetic demo matter overview and Customize view; matter Add Task; Tasks board and New Task; Calendar list and New Event; CRM directory, New client and a synthetic client detail; Intake pipeline.
- Opened and cancelled creation dialogs. No task, event, client, lead, message, paperwork, consent or preference was saved. Existing demo/validation records are not representative of customer workload; overdue counts are not a measure of customer behavior.
- Screenshots and accessibility observations were reviewed in this task. This report deliberately does not reproduce private client details.
- Read-only source audit reviewed current implementations and existing tests. No application tests were run because no product code changed. Backend payload persistence and successful provider sync were not tested live.
- The document-automation task owns acceptance of its merged #580/#582 changes. Its scope and interface constraints are recorded above; document engine, questionnaire and draft fixes are outside this epic.
- A companion [feature-parity audit](./research/core-ux-feature-parity-2026-09-22.md) (22 September 2026) reads the same source revision and records, for each capability across Matters, Tasks, Calendar, CRM/Intake and navigation, whether LawHand matches, partially supports, lacks or has not yet verified it — and whether a gap is a missing capability or an existing one users struggle to find. It also extends the [competitive research brief](./research/core-ux-competitive-research-2026-09-22.md) with Smokeball, Filevine, Rocket Matter, Actionstep, Centerbase and Lawmatics. Its gap register introduces UX-10 to UX-13 below.
- A [market and review evidence](./research/core-ux-market-and-review-evidence-2026-09-22.md) companion adds adoption figures (ABA 2024: practice-management availability fell 63% → 53%, solo use 45% → 37%; 74% of solos spend under $3,000/year), review aggregates, and practitioner complaint themes by capability. It is evidence for prioritising the parity gaps; it is not an independent market-share report and states its access limits.
- Existing UX planning/implementation PRs were inspected as background, particularly #395, #396, #425 and #431. Many old findings are already fixed. Before creating this draft, #395 was confirmed closed/unmerged and #425/#431 merged; current open PRs included #580 (document automation), #578 (ops) and #548 (Google root migration), with no duplicate core UX epic returned. This plan extends current main rather than reviving old branches. Remote branches alone do not prove active ownership.


### Source reconciliation after the research commits

The detailed parity register remains a dated audit of c4d80b46. Before this outline, main was refreshed to bcecb8cb0df6b9d60167be1bf9e05e15c22f5450 and the core source paths were compared. The following corrections take precedence when estimating implementation:

- **G-04 is partly addressed on main:** /matters search now matches matter name, matter number, client full name and client organization with a tenant-scoped lookup. Reuse this in UX-08/10. Attorney-name search and general document-content search are not provided by that change. The My Matters first-page cap and local filtering remain separate problems. [Current source](https://github.com/mattpainter701/lawhand/blob/bcecb8cb0df6b9d60167be1bf9e05e15c22f5450/backend/app/routers/matters.py#L583).
- **Task due-time audit wording needs precision:** the global task editor clears due_time when the due date is removed; it does not unconditionally clear it on every edit. The global form still lacks a due-time control. UX-03 should preserve timed tasks on unrelated edits and clear the time when the date is deliberately removed. [Current source](https://github.com/mattpainter701/lawhand/blob/bcecb8cb0df6b9d60167be1bf9e05e15c22f5450/frontend/src/pages/TasksPage.jsx#L338).
- **A calendar read/alert watcher is not court-rule calculation parity.** The audit's grouped “deadline/docket calculation” label must not be used as evidence that LawHand calculates jurisdiction-specific rule chains. That remains outside this epic.
- The document owner confirmed unchanged entry/return URLs and the preparation-completion semantics above. This source review does not certify production provider behavior or customer workflows.

## Current UX: what to retain and what to refine

| Area | Already working / worth retaining | Observed friction | Proposed refinement |
| --- | --- | --- | --- |
| My Matters | List/board choices; real matter links; keyword filtering; status tabs; sortable, resizable, hideable columns | At the inspected 1420px viewport, nine data columns plus Actions push deadline/status off the visible part of the table. Attorney metadata takes space before the next deadline. “5 matters need attention” is text rather than a direct queue entry. A second All Matters section adds another list model. | Everyday columns prioritize matter/client, next deadline, status, owner. Make Needs attention a view. Keep all fields available through Customize view. Give My/Firm scope one clear control where permissions allow. |
| Matter overview | Five primary tabs; client link; Quick Actions; Key Dates; task links; existing field visibility | Large description/budget header, onboarding card, empty message composer and expanded signature-creation form precede Quick Actions, Key Dates and To-Do. The existing gear cannot reorder these sections. | Compact identity; attention and work first; setup/actions opened deliberately. Let users reorder/collapse a finite set of overview sections using up/down controls. |
| Task creation | Existing API and matter context; optional date/time; assignment support | Matter modal defaults to Deadline; global form defaults to general. Due-time controls differ. Matter assignment control disappears when the team list is empty. | One shared task composer and clear assignment state; same plain labels and date/time behavior across entry points. |
| Tasks queue | My Work/Firm Work; board/list; due filters; workflow states; move menu alongside drag | Board can show many items in To Do with several empty columns. Risk counters are passive. Several filters live only in component state; no visible title search. | Everyday list with actionable Overdue/Today/Upcoming/Waiting views; board stays available. Preserve filters/scroll when returning; add bounded task search. |
| Calendar | Day/week/month/list, mobile agenda, event-type labels, task/provider deduplication, explicit drag intent | The inspected List begins with old entries and shows October entries under a September heading. No visible matter/type/source filter. “Deadline Calendar” describes only part of its content. | Calendar label, an accurate range, a useful upcoming agenda, a compact filter row, readable source/type labels. Preserve all current views and safe rescheduling behavior. |
| CRM / Intake | CRM uses the contact model; search and lifecycle filters; client-to-matter links; Intake already has five stages and Convert to Matter | “Call Intake” and “Intake” sit together without explaining their difference. CRM emphasizes four summary cards including SMS consent; new client exposes payment and lifecycle fields immediately. Filters are not URL-backed. | Clear cross-links for Clients, Contacts and Intake pipeline; existing lead handoff made explicit. Contact/next work first, optional administrative details second. |
| Navigation / preferences | Role presets, personal hide/reorder, sidebar sizing and collapse already exist | In the admin account, 18 navigation destinations are available. Personal navigation follows the user, while matter/column settings say “on this device.” Gear is near the profile, with a different customization surface on each page. | A consistent Customize view vocabulary, clear preference scope, opt-in everyday preset and easy reset. Preserve user choices and module/role authorization. |
| Cross-module search | Per-module search exists: matters, clients, contacts, firm memory, matter documents and the intake dashboard; current main expands matter search to matter number and client identity | No global search or command palette; the global chat shortcut creates a conversation with no input guard, so it can fire while typing in a form | Add a scoped search entry point in a later release; make the shortcut focus-safe now (UX-07, UX-10). |
| Bulk actions | — | No multi-select bulk reassign/status/archive on matters, and no bulk complete/reassign on tasks | Add permission-checked bulk actions with a confirmation summary and no partial silent failure (UX-11). |
| Task structure | Recurring, dependency and subtask concepts are absent; checklists exist only inside matter-workflow templates | Filevine Taskflow, Actionstep and Smokeball generate dependent and repeating tasks from matter phases | Validate demand before adding recurrence/dependencies; do not assume parity requires it (validated backlog). |

The Calendar range issue is source-confirmed: list uses the start of this month through the end of the next month, while its header uses only the pivot month's label. Treat the proposed upcoming default as a design choice, and the inaccurate range label as a correctness fix.

## User feedback: what does “Working on this” mean?

The user reports that staff ask what the button does and suggests it could show which matters are being worked on in a busy office. This is a concrete comprehension failure to include in baseline testing.

**Verified behavior in current main:** the button stores a manual boolean on an existing matter assignment. It remains set until someone switches it off. It does not start a timer, record a time entry, lock the matter, change its lifecycle status, add/remove its assignment, or determine whether it appears in My Matters. It has no dedicated focus timestamp or automatic expiry. The matter overview uses it for an Active Workers count and team labels. The Team panel still says Active / Set Active and Actively working, whereas the portfolio says Work on this / Working on this. The portfolio's hover hint explains part of the intent, but staff should not need hover help to discover a primary action's meaning.

**Hidden dependencies make this more than a wording change:**

- Research workspace routes require a flagged assignment for non-admin users who are not the matter owner. Clearing the flag can produce a 403 even while the user is still assigned to the matter. Workspace membership and write-role checks also apply; setting the flag alone does not grant every workspace permission.
- Chat document-reviewer fallback selection ranks flagged assignees ahead of other assignees. This can influence suggested/default reviewers; it does not directly reassign existing tasks or override explicit reviewer eligibility.
- The old active/watching board arrays are not the rendered board. The visible board uses matter lifecycle status. Do not reintroduce flag-based lifecycle movement.

These are source findings, not live permission or reviewer mutation tests. Their exact production behavior must be verified against the deployed revision.

**Recommended product contract:** preserve a lightweight, team-visible personal focus list, while making assignment, permissions and reviewer responsibility explicit and independent. Proposed copy:

| Surface | Proposed wording and behavior |
| --- | --- |
| Matter row secondary action | Add to my focus → Remove from my focus |
| Personal queue/filter | My focus; an additive view of accessible assigned matters the user has deliberately selected, leaving the assignment-based My Matters list intact |
| Shared indicator | On Alex's focus list; multiple names where appropriate |
| Short help beside the control | Keep this matter in your focus list and share that choice with your team. It stays here until you remove it. |
| Distinct concepts | Assigned to you = responsibility/membership; Status = case lifecycle; My focus = personal work selection |

Keep Open matter as the obvious way into the record. Show the shared focus indicator only to people already permitted to see the matter. Use a text/icon treatment that does not resemble online presence. Existing true values should remain selected after migration; do not reset staff choices. Do not label it “today,” “online,” “busy,” or “currently editing” without implementing the time/session behavior those words promise.

Before presenting this contract, remove the flag's hidden permission and reviewer-ranking roles under UX-01A. Research access must follow explicit tenant, assignment, workspace membership and capability rules, preserving documented owner/admin exceptions and denying unauthorized users. Default reviewers must follow an explicit responsibility/role policy; focus changes must not silently route review work. Review those policies at implementation kickoff rather than substituting an unrestricted membership check. Ordinary users may change only their own focus. Any manager/owner/admin action on another user's focus requires an explicit authority policy and a separately labeled, audited path; do not silently preserve the current generic team toggle.

A cosmetic rename alone cannot meet this contract. If the access work cannot fit, retain truthful existing semantics and defer the focus feature's rollout. Real-time office presence, automatic expiry, time tracking and work checkout are separate possible future products, outside this refinement epic.

## Gavel concepts applied to this epic

The [research brief](./research/core-ux-competitive-research-2026-09-22.md#gavel-workflows) documents the evidence and limitations. Gavel Workflows is most relevant to guided intake; its document-automation builder is not a replacement design for LawHand's everyday matter screen.

Apply a small set of interaction rules: each form has a clear purpose and next outcome; ask only relevant questions; show known client/matter information so it can be checked; put short help at the point of confusion; preserve input when validation or saving fails. Longer existing intake/conversion flows may use named steps and a review/edit summary. Simple task creation remains a short form.

UX-06 should test interruption and return at the CRM/document handoff using the document owner's existing persistence contract. “Saved” must mean the server confirmed persistence, and “Submitted” must identify the resulting record/action. If durable draft/resume is missing, record it as a separate dependency and coordinate with the PDF/intake task; do not imply that this epic already delivers a new draft or workflow engine. Calendar filters and matter navigation do not need interview-style steps.


## Proposed experience

**My Matters:** one recognizable workspace. Start with “My matters,” “Needs attention,” and “All accessible matters” as scope/view choices, with counts whose scope is explicit. The everyday table shows Matter/client, Next deadline, Status and Responsible person. Client can be a second line under the matter name on narrower screens. Filters and Customize view are adjacent. Secondary metadata stays available. Preserve existing assignments; introduce the secondary My focus action only after UX-01A decouples its hidden access/reviewer effects. My focus is separate from My/Firm scope and lifecycle status.

**Inside a matter:** compact identity and client; essential alerts; Add task / Add note / Add date actions; Next work and Key dates; Recent activity; Client communication and paperwork summaries; optional detail/billing information. Maintain the current primary tabs. Unread messages, failed delivery, expired/declined requests and overdue work remain visible as actionable summaries. Open the full composer or signature setup only when requested. Existing matters with unknown engagement status show a concise status prompt; never automatically mark them engaged.

**Tasks:** an everyday list for users who choose the everyday preset, preserving existing users' board/list choice. Overdue, Today, Upcoming and Waiting lead to filtered queues. Completed work remains reachable. Each row answers: what, which matter, who, when and status. Show the total when work is grouped. A task's recorded due date remains visible even when it appears in a waiting group. Use server search for the accessible corpus, not just the current page.

**Calendar:** upcoming agenda plus existing grid views. Users can narrow by matter, event kind and source where the backend supports the required scope. Clearly separate Task due, Appointment, Key date and Work block. Show local display timezone; date-only tasks stay date-only. A disconnected provider banner states what is unavailable while preserving local dates. Do not describe a successful local save as successful external sync.

**CRM:** clients and prospects are easy to find, with contact details, related matters and follow-up links near the top. Label existing Intake as “Intake pipeline” and Call Intake as “Call log & intake” provisionally; validate terminology with staff before changing labels. Offer explicit existing-record selection before creating a duplicate. Keep the contact model, record IDs, conflict decisions and current conversion endpoint. No drag into a new stage should silently accept a legal engagement.

## Parity audit: what it changes

The [feature-parity audit](./research/core-ux-feature-parity-2026-09-22.md) confirms the existing P0 scope and separates two things this epic previously blurred: **capabilities that are missing** and **capabilities that exist but are hard to find**. It changes scope in four ways:

- It confirms the P0 correctness fixes implied by the walkthrough: the calendar list-range label (G-14), a local save shown as success when provider sync failed (G-16), and the global chat shortcut firing while a form is focused (G-25). The first two belong to UX-05; the shortcut fix belongs to UX-07.
- It confirms the scale gaps already assigned to UX-08: the 100-record cap on personal matters and the 200-record task fetch.
- It adds two P1 stories — UX-10 (global cross-module search) and UX-11 (bulk actions) — and two validated-backlog items (UX-12 recurring/dependent/subtask work; UX-13 saved views). The P1 stories are Phase D follow-up candidates, outside the initial proposed core phases unless staff sessions show search or bulk work is a blocking failure.
- It records what must **not** expand this epic: document version history and prepared-document drafts (document-automation owner), stage-entry automation and automatic deadline shifting (excluded), ethical-wall policy authoring (security owner), and CRM-side portal/e-sign actions (document/client-portal owner).
- The [market and review evidence](./research/core-ux-market-and-review-evidence-2026-09-22.md) reinforces this prioritisation: practitioner complaints cluster on exactly the discoverability gaps this epic targets (constant task-filter changes, no firm-wide docket view, intake copy-paste, weak reporting), while the rules-based docketing leaders are specialists this epic explicitly does not try to match.

## Delivery backlog

Effort is a planning estimate in engineer-days including focused implementation tests, not a commitment. UX research, shared CI waiting and independent acceptance are additional. One implementation owner and one isolated branch/worktree per active implementation slice; stable story/task IDs link sequential slices. Shared-file work runs sequentially.

| ID | Priority / effort | User story and acceptance criteria | Dependencies / likely owner |
| --- | --- | --- | --- |
| UX-00 | P0 / 1–2 | **Baseline and validate the arrangement.** Run the seven scenarios below with 5 representative staff using synthetic records. Record success, time, wrong turns and assistance. Review a simple arrangement prototype with the product owner. Confirm primary persona and label choices. No customer-data recording without consent. | First; product/UX with engineer support |
| UX-01 | P0 / 2–3 | **Everyday My Matters.** At 1366×768 and 1440×900, the new/default everyday arrangement shows matter identity, next deadline and status without horizontal scrolling. Existing customized views retain their selected columns and may intentionally scroll. Needs attention opens the corresponding records. Keep column sorting, widths, existing preferences, board and lifecycle semantics. My/Firm counts identify their scope. Fix/remove the decorative View affordance in the secondary All Matters table. Search/filter state survives open → Back. | UX-00; frontend A |
| UX-01A | P0 prerequisite / 4–6 | **Decouple authorization/reviewer policy, then clarify the work signal.** Confirm and implement explicit research-access and default-reviewer policies independent of focus, then apply Add to my focus / Remove from my focus and shared list wording consistently in portfolio, overview and Team. Preserve existing selections. My focus is an additive filter over the current user's assigned matters; it never replaces the assignment-based access/list path or changes My Matters membership, lifecycle, deadlines or task ownership. Ordinary users can change only their own focus; any privileged on-behalf action is separately authorized, labeled and audited. Switching focus must leave legitimate research access and reviewer responsibility unchanged; unauthorized access must remain denied. No timer, lock or real-time-presence implication. | UX-00; backend owner first, frontend A afterward; agree policy with research/document-automation owners |
| UX-02 | P0 / 3–4 | **Matter overview arranged around work.** On the same laptop sizes, identity, actionable alerts and entry points to tasks/dates appear before setup forms. No empty full signature/message form in the everyday overview. Scope is the overview shell and entry-point presentation; preserve panel internals, delivery/signing behavior and the document owner's state contract. Move optional sections up/down, collapse/restore, reset; visible summary signals remain. Optional sections may be hidden but required alerts/identity cannot be. Values never disappear from Edit. Cancelling arrangement restores the prior layout. Keyboard and touch work without dragging. | UX-00; frontend A, after coordination with PDF task |
| UX-03 | P0 / 2–3 | **One task composer.** Open from global Tasks, a matter, or a matter-filtered queue: current context is visibly prefilled and correct. Global entry can select a matter. Title, assignee state and due date are easy to find; More options holds type/priority/reminders/notes as appropriate. Default general Task unless the entry explicitly says Deadline. Optional due time available consistently. No team members means an explicit Unassigned state or permitted staff search. Same payload semantics, linked-contact support where relevant, validation and server error handling. Prefilled client/matter context is visible and checkable; brief field help explains unusual choices. Two context-specific shells may remain if they share the composer contract. Failed saves retain input; repeated submit cannot create duplicates. | UX-00; frontend B + API review |
| UX-04 | P0 / 3–4 | **A queue users can resume.** Make risk counters actionable filters; add title search with an explicit new backend query parameter and scoped query tests; preserve view/scope/filters/page and return position. Everyday list is opt-in for existing users. Overdue and waiting work remains visible, with counts matching filtered data. Keep board transitions, reassignment, close reasons and review rules. A task created through UX-03 appears in the correct queue. Loading, truly empty, filtered empty and failed states differ. | UX-03; frontend B + backend |
| UX-05 | P1 / 2–3 | **A calendar that explains what is shown.** A regression test first proves the label matches the actual list range; upcoming agenda begins at today with a separate past/overdue route. Filter by matter, type and source; restore filters and view. Type labels and timezone are explicit. App-only save and provider-sync failure are distinguishable. Existing task dedup and move-deadline versus block-time confirmation remain. Editing dates works without drag. No automatic deadline shifting. | UX-03/04 data semantics; frontend B |
| UX-06 | P1 / 3–4 | **Clear CRM-to-intake handoff.** Expose links among Clients, Contacts and Intake pipeline without adding a new data silo. New client starts with identity/contact information; optional admin/payment fields expand on demand, consent is never inferred. Surface existing matches with distinguishing details before new creation. Lead conversion previews selected client/matter details, retains history/owner and handles retries without duplicates. Use short named steps only where they reduce a longer flow, show applicable questions, and allow review/edit before conversion. Validate existing interruption/resume behavior and truthful save/submission state; a missing durable draft facility is a separately estimated dependency. Back restores directory filters. Unclassified clients have an explicit filter/recovery path. Load Activity when opened rather than making every related record block the profile. | UX-03/04; frontend A + backend |
| UX-07 | P1 / 2–3 | **Predictable personal views and navigation.** Reuse existing personal navigation and role presets. Offer an everyday preset with the four core destinations prominent; review and apply deliberately, preserve existing custom order. All permitted modules remain discoverable. Use route links for browser new-tab/copy-link behavior. Scope shortcuts so typing in forms cannot start a chat or lose work. Match Customize view, Save/Cancel/Reset and scope labels across pages. Matter/column settings remain explicitly device-local for this release; navigation remains account-synced. | UX-01/02; frontend A |
| UX-08 | P1 / 3–5 | **Complete, honest lists.** Add paging/load-more and scoped totals for both My Matters and All accessible matters, CRM, and bounded task/overdue paths. The personal-matters endpoint needs a pagination/total contract; the all-matters and clients APIs already support paging and should be reused. Search examines all accessible matches for its documented fields. Demonstrate >100 assigned matters, >100 accessible firm matters, >100 clients, >100 contacts and >200 tasks with late matches, separately. Loading older results preserves filters and does not duplicate rows. An interrupted fetch cannot replace newer search results. No new cross-tenant access. | UX-00 contract first; API slices precede UX-01/04/06 consumers; backend + frontend integration |
| UX-09 | P0 release gate / 2–3 | **Usability acceptance and rollout.** Repeat the baseline scenarios with equivalent synthetic cases. Resolve failures; check keyboard-only use, 200% zoom, small viewport, restricted role, no data, dense data, failed request and disconnected calendar. Update short user guides. Pilot behind reversible presentation defaults, then expand after acceptance. Rollback restores prior views while preserving all business data and preferences. | All committed stories; QA/product |
| UX-10 | P1 follow-up / 3–5 | **Find a matter, client or contact.** One search entry point over the permitted corpus (matters, clients and contacts), with tenant/role scoping and explicit result types. Matches the accessible corpus rather than the current page. Loading, empty and failed states differ. No cross-tenant or restricted-matter leakage; denied by default. Document-content search stays with the firm-memory/document owner. | Parity G-24; backend + frontend A |
| UX-11 | P1 follow-up / 2–3 | **Act on many records safely.** Multi-select bulk reassign/status/archive on My Matters and bulk complete/reassign on Tasks, each permission-checked with a confirmation summary of what will change. No partial silent failure; the queue reflects results and counts stay consistent. | Parity G-05, G-12; frontend B + backend |
| UX-12 | P2 validated backlog / 4–6 | **Repeatable task structure — only if validated.** Recurring tasks, dependent/sequential tasks and subtasks, generated deterministically from a matter type or stage and visible in the queue. Do not start until staff sessions show demand; competitor parity alone is not a reason. | Parity G-13; needs UX-00 evidence |
| UX-13 | P2 validated backlog / 3–4 | **Named saved views.** Server-persisted named views for matters, tasks and reports that survive a device change, with clear scope labels and no permission widening. Deferred unless baseline testing shows users rebuild the same view repeatedly. | Parity G-28; backend + frontend A |

Revised estimated implementation: 27–40 engineer-days plus approximately 3–5 days of UX/independent QA effort. UX-01A is provisionally 4–6 engineer-days following review of its authorization and reviewer-policy scope; uncertainty is highest in permission-policy decoupling, server search/paging and existing-record conversion. A new durable draft/resume facility is not included. The parity audit adds two P1 follow-up stories and two validated-backlog items: including the P1 stories (UX-10, UX-11) the extended range is 32–48 engineer-days, while UX-12 and UX-13 stay explicitly outside both ranges until UX-00 evidence justifies them. Scope must be trimmed if discovery exposes backend gaps; do not call untested search or conversion complete to meet a date.

### Phased delivery proposal

One epic, several coherent releases. Use the outcome-gated phase sequence below. The existing 27–40 engineer-day range describes UX-00 through UX-09 including UX-01A; 32–48 includes UX-10/11. These are provisional effort ranges, not elapsed-time commitments for the whole future state. UX-12/13 and any newly requested capabilities require separate sizing and validation. Backend policy, paging/search, conversion and shared-file coordination are the largest uncertainties.

| Phase | Work and sequence | Exit condition |
| --- | --- | --- |
| **A. Establish the contract and fix misleading behavior** | UX-00 baseline plus a lightweight arrangement prototype. Define common labels, context/save/error and preference behavior. Pull the isolated calendar range/sync truthfulness and shortcut fixes from UX-05/07 forward; define completeness contracts from UX-08. Confirm the UX-01A authorization/reviewer policy before focus behavior changes. | User-test scenarios and priority failures recorded; agreed core/document interfaces; affected correctness checks pass. Market claims alone do not lock feature scope. |
| **B. Make the daily matter-to-task loop work** | UX-01/02/03/04 plus relevant UX-08 paging. Sequence shared files. Deliver find matter → understand next work → capture callback/task → see correct owner/date → return to queue. UX-01A is a dependency for focus changes, not a reason to block unrelated table/form improvements. | A usable end-to-end slice succeeds for representative novice staff, works with dense data and restricted roles, and survives Back and failed saves. No hidden first-page-only results. |
| **C. Connect the rest of the working day** | Remaining UX-05/06/07/08: calendar filtering/agenda, CRM-to-matter continuity, consistent navigation/preferences, client-profile responsiveness. Validate the existing document entry/resume/status handoff jointly with its owner. | Scheduling and existing-contact inquiry scenarios pass; daily work retains context across modules; the core UI accurately reflects external/document states. |
| **D. Add efficiency where scale warrants it** | UX-10 scoped cross-module record search, then UX-11 permission-checked bulk actions. Name the minimum useful operations and failure/retry policy before building them. UX-12 recurrence/dependencies and UX-13 named views enter only after demonstrated repeated-work needs. | Search covers the documented permitted corpus. Bulk outcomes identify exactly what changed and what failed, preserving task review and matter authorization rules. |

### Provisional effort allocation by phase

These are allocations of the existing story estimates, not new estimates to add to them. They support sequencing and staffing discussion; Phase A must revisit them after policy and usability discovery. They are engineer-days, not elapsed duration, and include no schedule contingency or CI/deployment waiting time.

| Allocation | Engineer-days | Story accounting; each budget counted once |
| --- | --- | --- |
| Phase A | 7–10 | UX-00 (1–2), UX-01A (4–6), and one day each drawn from UX-05 and UX-07 for their early correctness work. The ordered plan completes policy and focus UI in S2; any deliberately deferred integration still consumes this same reservation. |
| Phase B | 12–17 | UX-01 through UX-04 (10–14), plus the first UX-08 paging/completeness allocation (2–3). |
| Phase C | 6–10 | Remaining UX-05 (1–2), UX-06 (3–4), remaining UX-07 (1–2), and remaining UX-08 (1–2). |
| Shared release acceptance | 2–3 | One UX-09 engineering budget distributed across released slices and the final cross-module pass; do not charge it again for each phase. |
| Initial core total | 27–40 | Phases A–C plus the shared acceptance allocation. |
| Phase D, additional | 5–8 | UX-10 (3–5) and UX-11 (2–3), including their focused implementation validation. |
| Extended total | 32–48 | Initial core plus Phase D; UX-12/13 and unscheduled follow-ups remain outside this envelope. |

Independent UX/research/QA effort remains an additional 3–5 days overall, allocated during staffing rather than repeated per phase. UX-00 is engineering support for the baseline; UX-09 is engineering participation in acceptance/rollout. External participant/research effort is not counted as engineering capacity. If a pulled-forward fix exceeds its allocation, revise the affected story and totals; do not hide the overrun in a later phase or compress its acceptance work.

UX-09 is the acceptance and rollout gate for each released slice, with a final cross-module regression pass; it is not a testing phase postponed until the entire epic is built. Pull any data-loss, wrong-record, deadline, access or misleading-success defect ahead of optional presentation work. Use the seven baseline scenarios below and add a targeted scenario when a new capability enters scope.

Parallel lanes remain bounded: A owns matters and then CRM/navigation; B owns task composer/queues and then calendar; one designated backend owner sequences shared policy/API/schema work. Document-automation implementation remains a separate owner's lane. Existing worktrees stay immutable to other tasks.

## Ordered sprint execution plan

This section is the task-level execution plan for the UX stories above. All checkboxes are **planned, not completed**. The hierarchy is epic → phase gate → sprint → numbered task → implementation PR/evidence. UX-xx remains the scope/estimate identifier; Sx.yy identifies an executable task. The story table defines the outcome, this section defines implementation order, and [Usability validation](#usability-validation) remains the only source of scenario thresholds.

**Default sequence: S1 → S2 → S3 → S4 → S5 → S6 → core acceptance; then S7 → S8 only if Phase D is selected.** Tasks inside each sprint run in numbered order unless explicitly described as independent preparation. A completed prerequisite means its contract is agreed and, when code is required downstream, its implementation is merged, available in the integration environment, and verified at the recorded SHA. A draft PR or an unmerged endpoint is not a completed dependency.

Proposed cadence is two-week planning/review timeboxes once the team is staffed. The table allocates ordered scope, not eight guaranteed calendar dates: split a sprint if available capacity cannot accommodate its tested scope; keep its dependency order and do not advance an incomplete gate because the timebox ended. Engineer-days are summed effort across contributors, not elapsed days. Before Sprint 1, name the people filling FE-A (matters/CRM/navigation), FE-B (tasks/calendar), BE (API/access/data), product/UX and QA roles. One person can fill several roles; do not assume three engineers are available. Dates, velocity and concurrent capacity remain uncommitted.

### Sprint sequence and budget

| Order / phase | Demonstrable outcome | Budget drawn from existing stories | Engineer-days | Required predecessor |
| --- | --- | --- | --- | --- |
| **S1 / A** | Measured baseline, reviewed arrangement, truthful calendar/shortcut behavior | UX-00 1–2; early UX-05 1; early UX-07 1 | **3–4** | Kickoff and ownership check |
| **S2 / A** | Personal focus has explicit, independent access and reviewer semantics | UX-01A 4–6, including its UI and tests | **4–6** | S1 contracts |
| **S3 / B** | Find any permitted matter and understand its next work | UX-01 2–3; UX-02 3–4; UX-08 matter slice 1 | **6–8** | Phase A exit; matter API before its UI |
| **S4 / B** | Capture a task and work a complete, resumable queue | UX-03 2–3; UX-04 3–4; UX-08 task slice 1–2 | **6–9** | S3; task API before queue integration |
| **S5 / C** | Trust calendar dates and use consistent navigation/preferences | Remaining UX-05 1–2; remaining UX-07 1–2 | **2–4** | Phase B exit and task date semantics |
| **S6 / C** | Continue inquiry → follow-up → matter → document handoff | UX-06 3–4; UX-08 client/contact slice 1–2 | **4–6** | S5; directory/conversion contracts before forms |
| **Shared core acceptance** | Accepted releases plus final integrated core pilot | UX-09 one pool, allocated across S1–S6 and the core gate | **2–3** | Used throughout, not an extra implementation sprint |
| **Core total** | Phases A–C | Every UX-00–09 budget, including UX-01A, once | **27–40** | Core gate passed |
| **S7 / D, optional** | Search permitted matters, clients and contacts from one entry | UX-10 3–5 | **3–5** | Core accepted; Phase D selected |
| **S8 / D, optional** | Perform bounded bulk work with explicit per-record outcomes | UX-11 2–3 | **2–3** | S7; bulk operation policy |
| **Extended total** | Core plus selected Phase D | No UX-12/13 or unscheduled follow-ups | **32–48** | Phase D gate passed |

Budget reconciliation: S1 + S2 = Phase A 7–10; S3 + S4 = Phase B 12–17; S5 + S6 = Phase C 6–10. UX-08 is 1 + 1–2 + 1–2 = 3–5, not three full allocations. S2 includes all focus UI work; S3 only consumes it. Engineering tests and ordinary defect correction are in the implementation story estimates. UX-09 funds shared engineering participation in pilot/acceptance and rollout preparation once; independent UX/QA remains 3–5 days overall. S7/S8 include their own focused validation in UX-10/11, rather than reusing an exhausted core acceptance pool.

The task breakdown does not prove that the estimates are sufficient. At S1 exit, size the actual work with the named owners. Re-estimate before proceeding if policy requires additional migration, API/query performance needs substantial changes, conversion is not retry-safe, document-state endpoints are missing, or staff cannot complete the intended flow. Record the scope/effort change explicitly; do not absorb new engines or omitted testing into this envelope.

### Sprint 1 — establish the baseline and remove misleading behavior

**Lead:** product/UX with BE, FE-B for calendar and FE-A for shortcut/navigation. **Stories:** UX-00, early UX-05/07; F4/F7/F8; G-14/16/25. **Entry:** current main and deployed revisions identified, active work owners contacted. Contract discovery supports later stories; implementation budgets remain with those stories.

- [ ] **S1.01 — Reconcile current behavior.** Fetch current main, inspect overlapping worktrees/PRs and record the source/deployed SHAs. Recheck the expanded matter search, task due-time handling and latest document/cloud fixes. Mark each audit item still reproducible, already fixed, or unknown; remove already-landed work from implementation tickets.
- [ ] **S1.02 — Name owners and lock shared-file turns.** Assign people to FE-A, FE-B, BE, product/UX and QA; list the shared page/router/API files each sprint touches. Agree who edits MatterDetailPage, API exports, research access and reviewer fallback. Record document-owner dependencies without editing that owner's branch.
- [ ] **S1.03 — Prepare a reusable synthetic fixture pack.** Include duplicate client names, a restricted matter, existing focus on/off assignments, unassigned/waiting/overdue/no-date tasks, local and provider dates, and existing-client inquiries. Include separate dense sets beyond the current matter, client, contact and task caps with known late matches; keep these fixtures out of real client workflows.
- [ ] **S1.04 — Run the seven baseline scenarios.** Use the participant mix and measurement rules in Usability validation. Record result, duration, wrong turn and assistance per scenario. Explicitly ask participants to explain “Working on this” before suggesting replacement words. Capture observations without client text or identifiers.
- [ ] **S1.05 — Review the everyday arrangement.** Make a lightweight prototype of My Matters, matter overview, task form/queue and client handoff using existing visual components. Compare default hierarchy with current behavior on the target laptop sizes. Confirm where critical dates, ownership and required alerts stay visible; record rejected assumptions.
- [ ] **S1.06 — Write the shared interaction contract.** Specify labels and distinctions for assignment/status/focus/review, Clients/Contacts/Intake, saving/saved/failed/partial, local versus provider state, and account versus device preferences. Define visible context, cancellation, retry, Back and reset behavior. Keep default Task distinct from explicit Deadline entry.
- [ ] **S1.07 — Define endpoint completeness contracts.** BE records supported fields, authorization scope, deterministic ordering, bounded page size, total/count meaning and stale-request handling for personal/all matters, tasks/overdue/upcoming, clients and contacts. Reuse current APIs where adequate. Assign implementation to S3.01–03, S4.01–02 and S6.01–02; no new global search service here.
- [ ] **S1.08 — Reproduce the calendar range mismatch first.** Add a focused behavior check showing the list's actual start/end against its heading, including a month/year boundary. Fix the label or queried interval together so the UI makes one accurate promise; preserve current timezone and date-only handling.
- [ ] **S1.09 — Make calendar save/sync feedback truthful.** Exercise successful local persistence with successful, failed and unavailable provider synchronization. Show the saved local result and separate sync outcome; retain actionable retry/reconnect context and avoid duplicating an event. Consume any already-landed reconnect fix from the document owner; no OAuth rewrite.
- [ ] **S1.10 — Make shortcuts safe while typing.** Check text inputs, textareas, contenteditable controls, dialogs and composition input before handling the global chat shortcut. Preserve the intentional shortcut outside editing contexts. Verify ordinary typing cannot create a chat or discard a form.
- [ ] **S1.11 — Review the focus and document handoff contracts.** Identify all focus flag readers/writers for Sprint 2. Obtain the current preparation entry/resume/status/error contract from the document owner, including session correlation, serialized autosave/completion, preserved interview state during review, and failed draft completion after a successful save. Recheck owner-reported release/acceptance status; consume available contracts without taking over pending corrective preview work. Record owner decisions still needed before implementation.
- [ ] **S1.12 — Close the sprint with evidence and revised sizing.** Attach baseline results, prototype, glossary, API contract notes and correctness checks to the implementation plan/PRs. Apply the release checklist to any independently released fix. Reconfirm staffing and ranges before S2; carry unresolved policy questions as named blockers, not assumed behavior.

S1 capacity check: participant recruitment/sessions and external product/UX/QA coordination need named availability in addition to the 3–4 engineering days. Fixture/query-contract work must draw explicitly from UX-00 or the later owning story budget; track it once. If that work exceeds those allocations, revise the total at this gate rather than treating preparation as free.

**Exit/demo:** show accurate calendar range, distinguish local save from provider failure, type safely into a form, and review the measured daily-work baseline. The Phase A gate remains open until Sprint 2's policy work is verified.

### Sprint 2 — make personal focus mean exactly what it says

**Lead:** BE for policy and API, then FE-A for presentation. **Stories:** UX-01A; F8 and F1. **Entry:** S1.06/11 complete; research/access and document owners have agreed the replacement policies. This is an authorization/reviewer change before it is a labeling change.

- [ ] **S2.01 — Inventory the actual dependency graph.** Trace the assignment flag from model and update endpoint into research workspace checks, chat reviewer fallback, portfolio, overview and Team. List each use as focus display, access, reviewer selection or obsolete code. Recheck current main rather than relying only on the original source anchors.
- [ ] **S2.02 — Approve an explicit policy matrix with the responsible owners.** For own/other tenant, assigned/unassigned, matter owner/admin, workspace member/non-member and read/write capabilities, record allowed research actions. Separately define default-reviewer responsibility and eligibility. Make “focus on versus off produces identical access and reviewer outcome” an invariant.
- [ ] **S2.03 — Add behavior tests around those policies.** Use real API/database fixtures for allowed and denied matrix rows, especially an assigned non-owner clearing focus and an unauthorized user setting/forging it. Test read and write authorization separately and include existing owner/admin exceptions.
- [ ] **S2.04 — Decouple research access.** Replace focus-dependent access with the agreed tenant, assignment, workspace membership and capability checks. Preserve deliberate denials; do not substitute a broad membership-only rule. Verify both flag values produce the same authorized capabilities.
- [ ] **S2.05 — Decouple default-reviewer selection.** Implement the agreed responsibility/role policy without focus ranking. Preserve explicit reviewer choice, eligibility and artifact review/signature evidence rules. Prove changing focus neither changes existing review assignments nor redirects the next default selection.
- [ ] **S2.06 — Make focus mutation explicit.** Ordinary users can set their own selection idempotently. Define rejection of other-user changes and inaccessible matters. Default this release to self-management; only retain an on-behalf path if its authority, separate label and audit requirements are fully agreed and implemented within revised scope.
- [ ] **S2.07 — Preserve selections and compatible clients.** Keep existing true/false data as personal selections; document whether the storage field can remain while semantics change. If a migration is needed, reserve the next linear revision after current main and test upgrade/rollback compatibility. Do not rename storage merely to match copy.
- [ ] **S2.08 — Replace portfolio copy and behavior.** Use Add to my focus / Remove from my focus, with nearby explanation that the choice persists and is shared with permitted teammates. Preserve Open matter, assignment-based My Matters membership and lifecycle board placement. Failure restores the prior visible value and explains the retry.
- [ ] **S2.09 — Reconcile overview and Team.** Replace Active Workers/Set Active/Actively working wording with focus-list language consistently. Show named selections only within existing matter visibility. Remove presence-like indicators and ambiguous other-user toggles; keep responsibility/assignment controls distinct.
- [ ] **S2.10 — Add the personal focus view.** Apply it as an additive filter within accessible assigned matters. Make its selected state and count scope clear and carry it into the S3 paging contract. Test empty focus, multiple selectors, removal and return navigation without changing tasks, deadlines, assignment or status.
- [ ] **S2.11 — Validate comprehension and regression.** Run the canonical work-signal scenario, plus keyboard, restricted-role and failed-request checks. Use synthetic preexisting selections. Verify no timer, time record, lock, expiry or presence behavior is implied or created.
- [ ] **S2.12 — Complete the policy handoff and Phase A gate.** Give the document/research owners the exact policy PR and test evidence; update canonical authorization/MCP documentation as applicable. Verify the merged SHA in the integration environment before enabling focus presentation. Apply the release checklist.

**Exit/demo:** toggle focus and show unchanged research access, review responsibility, My Matters membership and lifecycle; show permitted team visibility and preserved selections. If decoupling fails, keep truthful existing behavior and defer focus rollout. Unrelated matter/table work may proceed only through an explicitly recorded, focus-free slice; do not claim Phase A/focus complete.

### Sprint 3 — make the matter workspace complete and understandable

**Lead:** BE first, then FE-A; document owner supplies status/entry contracts. **Stories:** UX-01, UX-02, matter allocation of UX-08; F1/F2/F6; G-01–04. **Entry:** Phase A passed, S1.07 matter contract agreed, shared matter files available.

- [ ] **S3.01 — Specify personal-matter paging precisely.** Carry assignment scope, lifecycle, focus, query, sort and stable tie-breaker through one contract. Define total as matching permitted records before pagination. Preserve callers that depend on the existing response until migrated.
- [ ] **S3.02 — Implement and test the personal-matter backend.** Remove the silent 100-record ceiling through bounded pages and scoped totals. Use synthetic assigned/unassigned and cross-tenant records; prove a matching assigned matter beyond the first 100 is reachable without exposing another user's restricted matter.
- [ ] **S3.03 — Reuse and integrate all-matter search/paging.** Use existing current-main fields: matter name/number, client full name and organization. State those supported fields beside search or in accessible nearby help; do not imply attorney-name, docket/court-number or document-content matching. Wire page/filter changes and loading state for personal and all-accessible views. Ignore stale responses, prevent duplicate rows and distinguish failed, filtered-empty and incomplete results.
- [ ] **S3.04 — Define attention counts from real predicates.** Document the included conditions and My/Firm scope. Clicking Needs attention opens precisely that matching corpus, not only loaded rows. Use existing authoritative data; if a predicate cannot be queried correctly, narrow the label or revise backend scope instead of presenting a fabricated total.
- [ ] **S3.05 — Apply the everyday table defaults.** Prioritize matter/client, next deadline, status and responsible person within the stated laptop widths. Preserve saved columns, widths and sort choices; offer preview/apply/reset for an optional preset. Keep additional metadata available and test deliberately wide custom layouts.
- [ ] **S3.06 — Unify scope and record entry.** Keep My matters and All accessible matters understandable, with focus as a separate filter. Remove/fix the decorative View affordance and redundant entry treatment. Use real matter links for keyboard, copy link and open-in-new-tab.
- [ ] **S3.07 — Restore list context.** Persist scope, query, filters, sort and page in the navigation contract; restore the return position after detail → Back. Handle reload, expired permissions and deleted records without showing stale unauthorized data or silently resetting a valid user's view.
- [ ] **S3.08 — Establish the compact matter header.** Show identity/client, responsibility, lifecycle and essential alerts before description/budget/setup detail. Keep full values available in existing detail/edit destinations. Do not infer engagement or change business data while rearranging.
- [ ] **S3.09 — Build the work-first overview shell.** Place next work, key dates and Add task/note/date entry points before optional setup. Link summaries to their actual records; distinguish no records from failed/unloaded data. Keep initial identity/actions usable while optional activity loads.
- [ ] **S3.10 — Consume communication/document summaries.** With the document owner, map unread/failed delivery, requests awaiting action, expired/declined states and resumable preparation to the existing canonical actions. Open full composers deliberately. Do not rewrite document panels, preparation state or review/delivery/signing logic.
- [ ] **S3.11 — Implement finite arrangement controls.** Provide up/down, collapse/restore, Save/Cancel/Reset and device-local scope text for allowed sections. Keep required alerts/identity visible and summaries actionable when collapsed. Preserve stored values, handle invalid old preferences, and make drag optional.
- [ ] **S3.12 — Validate dense, empty and restricted matters.** Exercise separate >100 personal and >100 accessible-matter fixtures, late search matches, failed loads, rapid filter changes, keyboard and 200% zoom. Run the canonical matter-finding and arrangement scenarios; capture any misleading summaries.
- [ ] **S3.13 — Demo and hand off the matter contract.** Show find → open → next work → arrange → Back against the merged backend. Give FE-B the visible matter/assignee/return-context contract for task creation. Apply the release checklist, including shared document entry/return validation when that shell changes.

**Exit/demo:** every permitted matter is reachable, the next action/date is understandable, and the user can rearrange optional sections and return to the same list. The end-to-end Phase B gate also requires Sprint 4.

### Sprint 4 — unify task capture and the daily work queue

**Lead:** BE for search/counts, FE-B for composer/queue; FE-A owns any coordinated matter entry change. **Stories:** UX-03, UX-04, task allocation of UX-08; F3/F6/F7; G-09–11. **Entry:** S3 entry/return contract and S1.07 query contract available.

- [ ] **S4.01 — Define task query semantics before the UI.** Add an explicit title-query parameter; specify My/Firm authorization, assignee, matter, status, date buckets and waiting/review state. Define stable sort/tie-breaker, page limits and totals from the same predicate used by the queue. Record date-only versus timed behavior.
- [ ] **S4.02 — Implement complete bounded task queries.** Update list and overdue/upcoming paths as needed; avoid a second task store. Add real HTTP/database tests with >200 tasks, late title matches, restricted matters and multiple tenants. Examine representative query plans/index needs and stop to resize if the small query change requires larger infrastructure.
- [ ] **S4.03 — Agree one task form/payload contract.** Map global Tasks, matter and matter-filtered queue entry points to visible prefill and a common validation/error model. Inventory edit behavior too, including preserving due time on unrelated edits and clearing it only when the due date is removed.
- [ ] **S4.04 — Implement the shared composer.** Put title, linked matter, assignee and due date first; expose consistent optional time. Default to general Task unless the invoking action explicitly means Deadline. Put type, priority, reminder and notes under appropriate optional controls without dropping stored values.
- [ ] **S4.05 — Handle assignment and context explicitly.** Show permitted choices or a clear Unassigned state if no team is available. Verify server-side assignment authority. Display prefilled matter/client/contact context; changing the matter clears incompatible prefill and does not quietly retain another client's details.
- [ ] **S4.06 — Make save/cancel/retry reliable.** Preserve values and focus after validation or network failure, prevent duplicate submission, and handle an uncertain response without blindly creating another task. Reuse existing retry/idempotency facilities; if the backend cannot meet no-duplicate acceptance, add and size that prerequisite. Cancel creates nothing.
- [ ] **S4.07 — Migrate the entry points in sequence.** First integrate global Tasks, then the matter entry, then the filtered-queue entry against the same contract. Preserve existing edit and linked-contact behavior. Validate equivalent inputs produce equivalent persisted tasks.
- [ ] **S4.08 — Build actionable work views.** Connect Overdue, Today, Upcoming and Waiting to explicit filters, with My/Firm scope and counts from S4.01. Show title, matter, owner, due date/time and state; keep actual due dates visible in waiting work. Missing dates and unassigned work remain discoverable.
- [ ] **S4.09 — Preserve review/workflow semantics.** Use existing statuses and authorized transitions, keep completed work reachable, and maintain keyboard move menus alongside board drag. Confirm that complete/reassign/wait actions update the source record and matching counters. A task needing review cannot bypass that requirement through a new shortcut.
- [ ] **S4.10 — Add search and bounded loading.** Connect server title search, clear filters, stable paging and explicit result totals. Show pending, truly empty, filtered-empty, failed and partial states accurately. Rapid searches cannot be overwritten by old responses; repeated pages do not duplicate tasks.
- [ ] **S4.11 — Preserve the user's queue position.** Retain view, scope, filter, search, sort/page and return position across open → Back and reload. Make the everyday list opt-in for existing board users; changing presentation cannot alter task status.
- [ ] **S4.12 — Verify the complete daily loop.** Run canonical callback and daily-work scenarios, task date/time and authorization suites, failed-save/retry checks, dense-data tests and narrow/keyboard layouts. Include capture from both entry points and a subsequent edit; do not accept mocked payloads as persistence evidence.
- [ ] **S4.13 — Close Phase B.** Demonstrate find matter → understand next work → add owned callback → see correct queue/date → open → return. Record usability failures and resolve blocking ones within the slice. Apply the release checklist; hand the verified task/date contract to calendar work.

**Exit/demo:** staff can capture and find the same correct task across matter/global contexts without losing their place, with complete counts and no hidden cap. Phase B does not pass while wrong owner/matter/date, duplicate creation or access leakage remains.

### Sprint 5 — make calendar and navigation predictable

**Lead:** FE-B for calendar, then FE-A for shared navigation/preferences; BE reviews supported filters. **Stories:** remaining UX-05/07; F1/F4/F7/F8; G-14–17/25–27. **Entry:** Phase B passed and task date/source semantics verified. S1 correctness fixes are reused, not rebuilt.

- [ ] **S5.01 — Reconcile event kinds and filter support.** Map existing task due dates, key dates, appointments and work blocks to displayed type/source labels. Confirm each proposed matter/type/source filter operates over the full permitted interval, not a partially loaded subset. Revise scope if the backend cannot support a truthful filter.
- [ ] **S5.02 — Make the agenda useful on entry.** Start the proposed upcoming agenda at today, provide an explicit route to past/overdue items, and keep day/week/month/list choices. Respect an existing saved view where supported; do not hide overdue legal dates by changing the default.
- [ ] **S5.03 — Integrate filters and navigation state.** Implement clear selected states, reset and accurate range headings. Persist the view/date anchor/filter state through details and Back. Distinguish no events from a provider or fetch failure.
- [ ] **S5.04 — Make date/source meaning visible.** Show the display timezone and preserve date-only semantics across local/provider rendering. Distinguish local task due dates from provider appointments; check daylight-saving transitions and month/year boundaries without inventing time on a date-only deadline.
- [ ] **S5.05 — Preserve safe editing and deduplication.** Test keyboard editing as well as drag. Keep explicit move-deadline versus block-work-time intent and existing task/provider deduplication. Verify making a work block leaves the original due date untouched and a retry does not create a second external event.
- [ ] **S5.06 — Verify disconnected-calendar recovery.** Show which source is unavailable while local dates remain usable. Use the current owner-provided reconnect path and return context; retest post-reconnect state rather than promising successful sync from authentication alone. Escalate an unresolved routing defect to its owner.
- [ ] **S5.07 — Consolidate customization language.** Apply Customize view and consistent Save/Cancel/Reset behavior to implemented matter/task/calendar surfaces where applicable. Clearly label account-synced navigation versus device-local matter/column layouts. Preserve preferences on failure and avoid silent reset during role changes.
- [ ] **S5.08 — Offer the everyday navigation preset.** Preview the four core destinations and existing permitted secondary modules before deliberate apply. Preserve custom order, make all authorized modules reachable, and provide a reversible reset. Hiding a navigation item never changes module access.
- [ ] **S5.09 — Use normal navigation links and safe shortcuts.** Check new-tab, copy-link, browser Back and focus behavior in the changed navigation. Reuse S1 shortcut protection. Ensure dialogs and unsaved forms are not bypassed by a newly introduced shortcut.
- [ ] **S5.10 — Validate and close the sprint.** Run the canonical scheduling scenario plus timezone, dedup, provider failure, keyboard and role/preset checks. Demonstrate a work block and unchanged deadline, then customizing/restoring the workspace. Apply the release checklist and pass the terminology/navigation contract to CRM.

**Exit/demo:** staff can explain the displayed range, event kind, source and save state, edit without drag, and restore their preferred navigation without changing permissions or business data.

### Sprint 6 — connect CRM, intake and document handoff

**Lead:** BE for completeness/conversion guarantees, FE-A for directory/forms; document owner for preparation-state interface. **Stories:** UX-06 and remaining UX-08; F5/F6/F7/F8; G-18–23. **Entry:** S5 terminology/return patterns agreed, current document entry and status contract available.

- [ ] **S6.01 — Verify client/contact listing contracts.** Reuse paginated clients and existing contacts endpoints; document each one's exact searchable identity fields, offset/page, total, stable ordering and visibility rules. Include an explicit way to recover unclassified clients. Do not add a duplicate CRM data model.
- [ ] **S6.02 — Wire complete directory loading.** Send real client/contact offsets and preserve query/filter/page while loading. Test >100 clients and >100 contacts separately, with late matches, duplicates in names and inaccessible records. Keep failures/partial results distinct from an empty directory.
- [ ] **S6.03 — Implement clear directory/pipeline relationships.** Apply staff-validated labels for Clients, Contacts, Intake pipeline and Call log & intake. Link the existing related screens and explain what each creates; preserve current routes/IDs and role authorization.
- [ ] **S6.04 — Simplify new-client capture.** Start with identity/contact information, visibly mark required fields and disclose optional administration/payment details. Preserve hidden values during edit. Never infer messaging consent or initiate outreach from creating/classifying a record.
- [ ] **S6.05 — Offer explicit existing-record selection.** Search the authorized corpus before creating a new identity; show distinguishing permitted details for similar names. Require a deliberate choice to reuse a record. Do not auto-merge people, reveal restricted matches or silently attach the inquiry to the closest name.
- [ ] **S6.06 — Put follow-up and related work first.** Surface current contact details, next owned follow-up and related matters. Use the S4 task contract to capture the follow-up with correct context. Load Activity when opened so optional history does not block the main profile.
- [ ] **S6.07 — Reconcile the conversion endpoint before changing its UI.** Verify preservation of inquiry history/owner and chosen client; document conflict-check/engagement requirements and allowed actor. Test double submission, uncertain network response and retry against the existing endpoint. If persistence cannot guarantee a single result, implement the bounded fix or revise the estimate before rollout.
- [ ] **S6.08 — Build review-before-conversion.** For the longer flow, use named steps only where helpful; show chosen client, matter details and responsible person before commit. Allow review/edit and explain the resulting record. A stage drag or view change cannot accept engagement, send messages or bypass a required conflict decision.
- [ ] **S6.09 — Preserve return and interrupted-work behavior.** Restore directory/pipeline filters/page and context on Back. Verify what the existing server actually saves before displaying Saved. Retain input after failure; distinguish an unsent form from a committed conversion. Record any missing durable draft capability as a separately sized owner dependency, not a delivered feature.
- [ ] **S6.10 — Integrate the document handoff.** Retain the agreed preparation route and template/set, matter, folder, session and return values. Preserve session owner/tenant/matter/template correlation and serialized autosave/completion; opening or closing review must retain interview state. Consume Documents in progress for resume; changing source matter clears incompatible prior-client data according to the document owner's contract. Do not implement a parallel questionnaire or draft engine.
- [ ] **S6.11 — Jointly test document result states.** With the owner, exercise matter → preparation → return/status, saved file plus failed session completion, and retry closing a draft without duplicate rendering. Display preview, durable save, preparation completion, approval, delivery and signature as distinct states; generating a preview alone cannot mark the document saved or complete. Use synthetic records; document internals and release acceptance remain owner responsibilities.
- [ ] **S6.12 — Validate the client journey and close Phase C.** Run the canonical existing-contact inquiry scenario plus restricted/duplicate identities, failed save, retry, cancellation and interruption. Verify directory/profile responsiveness, dense paging and zero accidental outreach/engagement. Apply the release checklist.
- [ ] **S6.13 — Run the integrated core pilot gate.** Use the shared UX-09 pool to repeat the canonical scenarios with equivalent cases, examine before/after observations, update the short guides and record pilot evidence. Confirm every core story's acceptance result and remaining issue owner before broader rollout; follow the core release gate below.

**Exit/demo:** reuse an existing contact → assign a follow-up → review conversion → open the correct matter → enter/resume document work → return to an accurate status and the prior queue. Core completion requires the integrated pilot, not merely individual screen screenshots.

### Sprint 7 — optional cross-module record search

**Lead:** BE then FE-A. **Stories:** UX-10; F6; G-24. **Entry:** core accepted, Phase D selected, and staff evidence justifies this additional efficiency scope. Search remains matters, clients and contacts; document-content retrieval/ranking has a different owner.

- [ ] **S7.01 — Define the smallest useful search contract.** Record searchable fields per result type, permission scope, result limits/paging and type labels. Reuse the S3/S6 endpoints where possible. Do not promise attorney, docket or document-content fields not supported by an agreed implementation.
- [ ] **S7.02 — Specify aggregation and failure behavior.** Define ordering, duplicate handling where the same identity has multiple roles, request cancellation and partial-source failures. A failed client query cannot look like “no clients”; never expose restricted-record existence through totals or snippets.
- [ ] **S7.03 — Implement scoped queries with real authorization tests.** Exercise cross-tenant, restricted-matter and changed-role fixtures, including matching records beyond each first page. Use bounded payloads and verify server authorization rather than trusting client filtering.
- [ ] **S7.04 — Build a clear search entry.** Provide an accessible input, explicit record-type results, keyboard navigation and normal record links. If a shortcut is added, apply the existing editing/input guard. Loading, empty, failed and partially available results need distinct wording.
- [ ] **S7.05 — Preserve result and return context.** Open a selected record with the appropriate destination and retain the originating search on Back. Cancel stale searches and avoid displaying an old query's results under the new input.
- [ ] **S7.06 — Validate a record-search scenario.** Add a scoped scenario to the canonical validation table before pilot: locate a known late-page matter/client/contact, distinguish similar identities and return to results. Agree its target with product/UX at entry; do not retroactively set a passing threshold.
- [ ] **S7.07 — Release the bounded search slice.** Attach contract, authorization/dense-data evidence and pilot outcome. Apply the release checklist and keep firm-memory/Cloud Search diagnostics and ranking changes outside this implementation.

**Exit/demo:** one entry finds the intended permitted record across the three declared types and preserves the search on return; forbidden records and hidden totals never leak.

### Sprint 8 — optional bulk work with clear consequences

**Lead:** BE for operation policy/results, then FE-B for Tasks and coordinated FE-A for Matters. **Stories:** UX-11; F3/F8; G-05/12. **Entry:** S7 passed, stable queues/counts available, and product owner has selected the minimum useful bulk operations. No bulk deadline moves.

- [ ] **S8.01 — Agree the operation/authority matrix.** For matter reassign/status/archive and task complete/reassign, define eligible records, actor permissions, review/engagement constraints and audit needs. Preserve existing transition validation. If one operation requires a larger policy change, separate and resize it rather than bypassing the rule.
- [ ] **S8.02 — Define selection scope.** Start with explicitly selected record IDs and a visible selection count; define behavior across page/filter changes. Clear or visibly reconcile selection when the corpus changes. Do not imply “all matching records” unless a separate bounded server contract is intentionally implemented.
- [ ] **S8.03 — Define execution and retry semantics.** Set an operation size limit, per-record authorization/revalidation and a result model with success, failure and reason. Handle concurrent edits and ambiguous timeouts without reapplying completed transitions or duplicating side effects. Offer retry for eligible failures only.
- [ ] **S8.04 — Implement server behavior and tests.** Cover mixed permitted/forbidden records, different tenants, deleted/changed records, stale status and partial failure. Test the actual database outcome and audit trail; task completion still respects review requirements.
- [ ] **S8.05 — Implement matter selection and confirmation.** Show exactly which selected matters and fields will change, plus excluded/ineligible records. Use explicit action verbs; cancellation has no effect. Keep selection accessible by keyboard and understandable on narrow layouts.
- [ ] **S8.06 — Implement task selection against the same contract.** Reuse shared interaction rules while keeping task-specific ownership/review validation. Preserve queue scope and prevent repeated submission while the operation is pending.
- [ ] **S8.07 — Display and reconcile each result.** Separate successes from failures, link to affected records and update authoritative counts. Keep failed items identifiable; no blanket success banner after partial failure. Offer undo only where the operation can truly restore the prior state.
- [ ] **S8.08 — Validate the bulk-work scenario.** Add a representative mixed-success/cancellation/retry scenario and its product-agreed target to the canonical validation table before pilot. Verify comprehension of selection scope and consequences, plus access/review invariants.
- [ ] **S8.09 — Close Phase D.** Demonstrate a bounded operation with one rejected record, precise results and a safe retry. Apply the release checklist and record evidence for both search and bulk follow-ups. Do not pull UX-12/13 into the release merely because the core interface is cleaner.

**Exit/demo:** staff can predict what will change, see exactly what succeeded and recover from a partial failure without changing unauthorized records or bypassing review.

### Release checklist applied to each implementation slice

These repeatable gates are work within the relevant implementation story and shared UX-09 allocation, not eight extra budgets. Product/UX owns usability acceptance, each implementer owns focused tests, QA owns independent checks, and the designated release owner owns any authorized deployment.

- [ ] **R.01 — Ready to implement:** record task/story IDs, predecessor contract/SHA, named owner, current-main starting SHA, isolated branch/worktree, shared-file ownership and the demonstrated failure/outcome. Resolve affected policy decisions before dependent edits.
- [ ] **R.02 — Ready for review:** finish behavior and focused tests, real API/database checks where data/access is involved, keyboard/zoom/narrow-viewport checks, and empty/dense/failed/restricted states relevant to the slice. Record evidence against the task IDs and canonical scenarios.
- [ ] **R.03 — Ready to merge:** semantically update against current main; inspect the final diff, migrations if any, required documentation and correctly sequenced release notes. Complete the PR attestations, push, and wait for all required CI and the merge gate on the final head. Shared owners review changed interface contracts.
- [ ] **R.04 — Ready to pilot:** after authorized merge, verify the exact merge SHA and integration environment. Record enabled presentation defaults, rollback method, owner, pilot audience and permitted synthetic/live validation scope. A staged UI demo does not prove provider persistence or access correctness.
- [ ] **R.05 — Ready to expand:** record canonical scenario results and related regression results. Any wrong matter/deadline, unintended send, lost input, duplicate committed action or access leak blocks rollout. Resolve the failure or explicitly withhold the affected feature; averages do not waive these failures.
- [ ] **R.06 — Handoff and retain evidence:** update short user instructions, release entry and the story/task status with PR/commit/test links. Record remaining issues, preferences/data compatibility and the exact rollback boundary. Verify release acceptance where deployment is authorized, then retire only the completed clean worktree under repository policy.

Presentation rollback can restore an old arrangement while preserving user data/preferences. Authorization decoupling is different: do not reactivate the old focus-dependent access/reviewer behavior by casually reverting the whole sprint. Agree policy/data compatibility and a controlled recovery path with the responsible owner before releasing Sprint 2.

### Core acceptance, defer rules and implementation handoff

The core gate follows Sprint 6 and closes UX-00–09, including UX-01A. Check each story's acceptance against the merged implementation, repeat the canonical staff scenarios and cross-module regression, and record whether the result is accepted, withheld or still blocked. The target dates are set only after owners and realistic sprint capacity are known. No implementation checkbox can be checked because its design text was written.

If a dependency slips, move only independent preparation forward (fixtures, prototypes, documentation and tests against agreed contracts). Keep shared-file edits, API consumers and merge order sequential. To release unrelated matter/task improvements while focus is blocked, document the trimmed slice and retain existing truthful focus behavior. Never quietly waive an authorization, data-completeness, duplicate-prevention or deadline-safety gate.

UX-12 recurring/dependent tasks and UX-13 named saved views remain discovery backlog: capture the repeated workflow, validate demand, define minimum behavior and dependencies, then separately size and schedule them. Personal cloud recovery and Cloud Search observations remain unscheduled owner-coordinated follow-ups. Neither competitor parity nor an unused sprint slot brings them into this budget.

At sprint kickoff, copy only the selected Sx.yy tasks into implementation issues/PR checklists, retaining their IDs and story links. Track named owner, status, blocking dependency, PR/merge SHA and acceptance evidence there, then link the result back here. This planning update does not create implementation branches or issues, enroll staff in a pilot, merge this draft, or begin product changes.


## Backend reuse and exact search scope

Current APIs already provide paginated all-matter listing (`page`, `page_size`, `total`) and client listing (`q`, `limit`, `offset`, `total`). Reuse them. At the reconciled main revision, all-matter search matches matter name, matter number, client full name and client organization. Reuse it; do not promise attorney-name, docket/court-number or document-content matching without a verified contract. Task title search is new work; task listing already has `limit`/`offset`, while the overdue endpoint is unbounded. The personal `/matters/my` endpoint has a separate hard limit of 100 without pagination or total. UX-04 owns the task title-query change; UX-08 owns complete paging/totals and bounded overdue queries. Test tenant/role scope and dense-data query performance. No new schema is assumed for existing matter/client paging; assess indexes for new task search before committing its estimate.

## Usability validation

The table below is the single source of truth for scenario completion targets; fundamentals and phases reference it rather than restating thresholds. Use 5 participants initially: 3 paralegals/assistants, 1 attorney, 1 intake staff member, adjusting to the user-selected primary role. Use realistic synthetic cases with duplicate names, one restricted matter, overdue tasks, no-date tasks and a disconnected calendar. Let users work without coaching; record each assistance request. Counterbalance equivalent cases between before/after sessions to reduce practice effects.

| Scenario | Completion criteria | Proposed target (not an observed result) |
| --- | --- | --- |
| Find a matter and its next deadline | Correct matter and date; identify missing/overdue state correctly | 4/5 unassisted within 30 seconds |
| Understand the work signal | Explain what selecting/removing focus does, who can see it, and whether it starts time tracking or changes assignment | 4/5 explain correctly without hover help; nobody assumes it locks the matter or reports live presence |
| Capture a callback from the matter | Correct matter, owner and due date; no duplicate | 4/5 unassisted within 60 seconds |
| Decide what to do today | Find own urgent/due work and open one item | 4/5 unassisted within 30 seconds |
| Arrange the workspace | Move Key dates above Activity, collapse an optional section, restore defaults | 4/5 unassisted within 90 seconds; no lost values |
| Schedule and distinguish a work block from deadline | Create/edit the intended item; actual deadline unchanged when blocking time | All participants preserve the correct deadline; 4/5 unassisted |
| Handle a new inquiry for an existing contact | Reuse the correct contact, set next follow-up, understand conversion into a matter; separately probe interruption/return and whether save/submission state is understood | 4/5 unassisted within 2 minutes; no accidental outreach or duplicate |

Secondary targets: at least 30% lower median completion time versus measured baseline; task ease score median ≥5/7; fewer wrong destinations/assistance requests. This small formative sample finds usability problems; it does not establish statistical prevalence. Any wrong matter, wrong deadline, unintended send, lost input or access leak blocks rollout even if average speed improves.

Collect event name, elapsed duration, role category and success state only if telemetry is added. Do not log names, task text, message bodies, searches or document contents. Manual moderated measurement is sufficient for Phase A; analytics infrastructure is not a prerequisite.

## Engineering validation and completion

Use focused existing suites and add behavior tests where changes warrant them:

- Matters: MyMattersList, MyMattersActiveSemantics, MatterPortfolioBoard/Triage, MatterListColumns, MatterNavigation, MatterViewGear; browser open/back and responsive checks.
- Focus semantics: real API/database tests for both flag states with tenant separation, own/other assignment, owner/admin exceptions, authorized workspace member and non-member. Focus changes cannot add/remove research access, change default reviewer responsibility or create time/task records. UI copy and counts agree across portfolio/overview/Team. Preserve prior selections and deny unauthorized focus edits.
- Tasks: TasksPage and TaskBoard; both creation entry points; assignment authorization; date-only/time handling; keyboard transitions; same input retained after failure; duplicate-submit protection.
- Calendar: CalendarPage scheduled-events, timezone, task-drag, CalendarTaskDeduplication; accurate agenda range and filters; provider failure alongside local dates.
- CRM/navigation: ClientsPage, ClientDetailPage, IntakePage, navigation/NavigationEditor/Sidebar; identity reuse, restricted results, cancellation, stale responses, URL restoration and role changes.
- API search/paging/conversion: actual HTTP/database tests with synthetic tenant-separated fixtures, boundary sizes and retry assertions. Do not rely exclusively on mocked UI responses.

Before each implementation story: fetch/prune origin, inspect existing branches/worktrees/PRs, compare with current main, confirm ownership and clean state, then create one task branch/worktree from origin/main. Record starting SHA and affected shared files. Do not recover stale branches merely for their unique commits.

Before completion: semantically update from current main; review final diff; focused tests and required CI; correct customer release sequence where applicable; exactly one documentation and release-note option in the PR template plus security/privacy attestation; push/open PR; wait for all required checks and the merge gate at the final head SHA; merge only with fresh green CI; verify exact merge on origin/main; perform authorized staged/production acceptance; remove only the completed clean worktree under retention policy. Use the current deployment skill when deployment is actually requested/undertaken. No deployment is part of this planning pass.

## Decisions for collaboration

1. Confirm the first audience. Proposed: assistants/paralegals, with attorney and reception validation.
2. Confirm the first end-to-end outcome. Proposed: find a matter, understand next work, capture an owned task and return to the queue; address misleading state and incomplete lists in that sequence.
3. Confirm customization scope. Proposed: finite section order/collapse and clear presets, with account sync for existing navigation and explicit device-local matter layouts in release one.
4. Confirm release scope and staffing after Phase A. Use the four phase gates and ordered S1–S8 task plan; choose sprint dates only after scope, dependencies and named staffing are understood.
5. Confirm the focus-list contract. The user supplied the first concrete confusion example: “Working on this.” Proposed: a durable team-visible personal selection, with permissions/reviewer responsibility decoupled. Test comprehension before expanding this signal into live office activity.

These are product tradeoffs for the collaboration the user requested. No extra policy approval is required to continue reversible planning. Implementation follows the agreed scope; this document is not a claim that code has shipped.

## Source map and publication state

See the [competitive research brief](./research/core-ux-competitive-research-2026-09-22.md) for vendor documentation, user-review evidence and limitations, the [feature-parity audit](./research/core-ux-feature-parity-2026-09-22.md) for the per-capability match/partial/gap register and its source appendix, and the [market and review evidence](./research/core-ux-market-and-review-evidence-2026-09-22.md) for adoption data and practitioner complaint themes.

Current source anchor examples (immutable audit snapshot):

- [Working toggle and current hint](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/pages/MatterPortfolioPage.jsx#L69)
- [Persisted assignment flag](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/backend/app/models/matter_assignment.py#L50)
- [Flag update endpoint](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/backend/app/routers/matters.py#L1635)
- [Research workspace access dependency](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/backend/app/routers/research_workspaces.py#L50)
- [Document-reviewer fallback ordering](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/backend/app/services/chat_tools/handlers.py#L664)

- [My Matters list and loading](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/pages/MatterPortfolioPage.jsx#L675)
- [Column configuration](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/components/matters/MatterListColumns.jsx#L132)
- [Matter overview ordering](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/pages/MatterDetailPage.jsx#L1065)
- [Matter task composer](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/components/AddTaskModal.jsx#L19)
- [Task queue state and loading](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/pages/TasksPage.jsx#L1175)
- [Calendar implementation](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/pages/CalendarPage.jsx)
- [Client directory loading](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/pages/ClientsPage.jsx#L140)
- [Navigation preferences](https://github.com/mattpainter701/lawhand/blob/c4d80b46522e1c2b9436fdc8fae343c9d78db8c9/frontend/src/components/NavigationEditor.jsx)

This documentation branch was originally created from origin/main at c4d80b46522e1c2b9436fdc8fae343c9d78db8c9, also the original audit snapshot. The later source reconciliation used main at bcecb8cb0df6b9d60167be1bf9e05e15c22f5450 without merging product code into this draft. Future implementation must start from then-current origin/main. Branch docs/core-ux-future-state-plan remains owned by Plan legal platform UX refinement. It adds this epic and its three research companions (competitive research, feature-parity audit, market and review evidence) only. No application files, migrations, provider settings or customer release notes change. Runtime tests and production acceptance belong to later implementation PRs; the draft must remain open for collaboration without merging or enabling auto-merge.
